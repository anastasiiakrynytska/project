<div class="card">
    <img src="{{ img }}" class="card-img-top" alt="{{ alt }}">
    <div class="card-body">
        <h2 class="card-title">{{ title }}</h2>
        <p class="card-text">{{ description }}</p>
    </div>
</div>